#pragma once

#include <string_view>

// Given two strings, for example 'AXYUIZ' and 'CXYZAE',
// find the length of the largest common subsequence of both strings,
// e.g. in the example it is 'XY' with a length of 2.
namespace challenge
{
    [[nodiscard]] constexpr int get_length_of_longest_common_sequence(std::string_view left, std::string_view right)
    {
        left;
        right;
        // TODO implement a solution
        return -1;
    }
} // namespace challenge